\documentclass[english, a4paper, 11pt, twoside]{article}

% -----------------------------

%
% config
%
\input{config.tex}

% -----------------------------

%
% preamble
%
\usepackage{./cldh/preamble}
% inputenc and fontenc already loaded in preamble.sty
% -----------------------------

\begin{document}

\input{cldh/frontpage}
%\includepdf[pages=-]{./cldh/Eigenstaendigkeitserklaerung_Englisch.pdf}
\newpage

%\tableofcontents
\newpage

% -----------------------------
% -----------------------------
% BEGIN 
\pagenumbering{arabic} %all styles: arabic, alph, Alph, roman or Roman

\begin{abstract}
Multilingual foundation models represent a central trend in modern machine learning, yet pretraining across massive corpora often encodes social biases. While previous studies have demonstrated that static embeddings distilled from contextualized models encode gender bias in English, Spanish presents a particular challenge as most professions are morphologically gender marked, confusing grammatical gender with social bias. 

This study evaluates how static embeddings extracted from multilingual models assign gender bias to grammatically invariant Spanish professions (e.g., \textit{dentista}, \textit{taxista}) without explicit supervision. Comparative analysis is conducted
across Arabic and Tigrina. 

Using geometric test across multiple architectures, we demonstrate that while models successfully encode the grammatical neutrality of certain suffixes, statistical frequency and societal bias override morphological rules for fields historically dominated by men. Finally, our results demonstrate that the internal design of the model also has an influence bias direction. 


\end{abstract}

\section{Introduction}\label{section-introduction}
Large language models trained across numerous languages represent a central trend in modern machine learning. Models such as mBERT and the XLM RoBERTa family create shared representational spaces for dozens of languages. This compression raises an important structural question, when languages with radically unequal training data coexist, how do these models distribute social associations across them? \textcite{Bommasani:2020} demonstrated that static embeddings distilled from English contextualized models encode social biases. However, English nouns generally lack grammatical gender. In Spanish, is very common to find nouns marked by gender (e.g., \textit{maestro/maestra}), which often confuses grammatical gender with social bias.

This research investigates how these models handle grammatically neutral Spanish professions compared to those with explicit gender markers. We also place Spanish within a broader comparative context alongside languages with lower training resources, such as Tigrinya and Arabic. By examining these models across their entire depth, we aim to understand how gender bias varies from the initial input to the final output. Previous studies establish that early layers process syntax while middle layers encode semantic relationships, which is exactly where we expect social associations to peak before a final representational collapse at the output layer.

Furthermore, we evaluate the robustness of our geometric bias measurements. We test whether a projection estimator sensitive to magnitude and a cosine estimator based entirely on angles agree on the underlying trends. We observed a Spearman correlation exceeding 0.96 across all valid models, confirming the reliability of our mathematical approach.

This research aims to fill the identified gap with a layer wise, cross linguistic and architecture comparative, with Spanish as the main focus, following the questions:
\begin{itemize}
    \item \textbf{RQ1}: Do multilingual transformers encode measurable gender bias in Tigrigna, Arabic, and Spanish under a unified geometric protocol?
    \item \textbf{RQ2}: How does measured bias vary across transformer layers, and at which depths do associations peak?
    \item \textbf{RQ3}: Do two independent geometric estimators agree on bias trends across models?
    \item \textbf{RQ4}: Does attention architecture determine gender bias direction, independent of pretraining data?
\end{itemize}

\section{Related Work}\label{section-related-work}
\subsection{Bias in Static Word Embeddings}
Early work established that social biases are encoded geometrically in static word embeddings. \textcite{Bolukbasi:2016} derived a gender direction from anchor sets, revealing systematic profession gender associations and proposing projection based debiasing. \textcite{Caliskan:2017} introduced WEAT as a statistical
framework for implicit associations; \textcite{Garg:2018}  showed embedding bias correlates with historical
trends. \textcite{Gonen:2019} demonstrated that removing a single bias direction does not eliminate
deeper geometric bias, motivating continued geometric analysis.

\subsection{Interpreting Contextual Representations via Reductions}
Transformer models produce contextualised representations that vary across inputs and depth, complicating direct application of geometric methods. \textcite{Bommasani:2020} formalised reductions of contextual embeddings to stable word level vectors suitable for geometric analysis, while \textcite{Rogers:2020} surveys the unequal distribution of linguistic information across transformer layers, motivating layer wise analysis.

\subsection{Multilingual Bias and Cross Lingual Considerations}
As multilingual transformers became widely adopted, bias analysis extended beyond English. \textcite{Zhao:2019} and \textcite{Zhao:2020} showed that geometric bias methods transfer to contextual multilingual models and that bias patterns vary substantially across languages, with debiasing in one language not generalising to others. \textcite{Guo:2021} further showed contextualised embeddings encode intersectional associations. These studies predominantly evaluate final layer representations. \textcite{Lauscher:2020}
showed zero shot cross lingual transfer degrades substantially for typologically distant and low resource languages. 


\subsection{Architecture Driven Effects on Representations}
The models evaluated here differ not only in scale but in fundamental attention design. \textcite{He:2021} introduced Disentangled Attention, which separates content and positional information into independent attention matrices, substantially changing how contextual relationships are encoded across layers. \textcite{He:2023} extended this design to multilingual pretraining via an ELECTRA-style training objective, producing the microsoft/mdeberta-v3-base checkpoint evaluated here. \textcite{Liang:2023} enlarged the Sentence Piece vocabulary from 250K tokens (XLM-R) to 1M tokens (XLM-V) to improve tokenisation coverage for
low resource languages. These architectural and vocabulary decisions constitute the design space whose bias consequences this study directly compares.

\subsection{How This Work Differs}
We integrate geometric bias analysis  \textcite{Bolukbasi:2016} with contextual-to-static reductions \textcite{Bommasani:2020} in a controlled layer wise, architecture comparative study across five multilingual models, revealing that Disentangled Attention produces a complete reversal of gender bias direction not previously documented in the multilingual bias literature.


\section{Methodology}\label{section-methodology}

\subsection{Models}
We evaluate four pretrained multilingual transformer models without fine tuning. All checkpoints are loaded from HuggingFace Hub; no parameter updates are performed.
\begin{itemize}
    \item \textbf{mBERT} (\texttt{bert-base-multilingual-cased}; \cite{Devlin:2019}): 
    A 12-layer Transformer model (yielding 13 hidden states including the embedding layer). It employs a WordPiece vocabulary of approximately 120K tokens covering 104 languages. It serves as our baseline for multilingual pre-training.

    \item \textbf{XLM-RoBERTa-base} (\texttt{xlm-roberta-base}; \cite{Conneau:2020}): 
    Consists of 12 layers (13 hidden states) with a 250K-token SentencePiece vocabulary. It was trained on 2.5TB of CommonCrawl data, significantly scaling the training data compared to mBERT.

    \item \textbf{XLM-RoBERTa-large} (\texttt{xlm-roberta-large}; \cite{Conneau:2020}): 
    A larger variant featuring 24 layers (25 hidden states) and a hidden dimension of 1024. It utilizes the same 250K SentencePiece vocabulary as the base model but provides higher capacity for complex linguistic patterns.

    \item \textbf{XLM-V-base} (\texttt{facebook/xlm-v-base}; \cite{Liang:2023}): 
    This model maintains a 12-layer architecture (13 hidden states) but features an expanded SentencePiece vocabulary of 1M tokens. This design choice aims to solve the "vocabulary bottleneck" observed in low-resource language processing.

    \item \textbf{mDeBERTa-v3-base} (\texttt{microsoft/mdeberta-v3-base}; \cite{He:2023}): 
    Comprising 12 layers (13 hidden states), this model utilizes \textit{Disentangled Attention}. Unlike the standard Transformer architecture which sums content and position embeddings, DeBERTa represents them separately. The attention weight $A_{i,j}$ between token $i$ and $j$ is calculated as:

\end{itemize}

\subsection{Languages and Data}

This study focuses on Spanish (\textit{es}). We utilize a sentence level corpus derived from \textbf{Spanish Wikimedia}, containing 100,000 sentences to provide a broad representation of natural language patterns.

\textbf{Gender Anchors:} To define the semantic gender direction, we employ a set of definitional anchor pairs (e.g., \textit{él/ella}, \textit{hombre/mujer}). These pairs provide the reference points for calculating the gender subspace in the embedding layers \parencite{Bolukbasi:2016, Zhao:2019}.

\textbf{Profession Lists:} To isolate morphosyntactic properties from latent societal bias, we organize two distinct target lists of profession terms:
\begin{itemize}
    \item \textbf{Group A (Invariant):} Grammatically neutral terms that remain identical regardless of the referent's gender (e.g., \textit{dentista}, \textit{comerciante}, \textit{estudiante}, \textit{periodista}). 
    \item \textbf{Group B (Marked):} Grammatically gendered terms that possess distinct masculine and feminine forms (e.g., \textit{ingeniero/ingeniera}, \textit{abogado/abogada}). These serve as a baseline to establish the expected magnitude of the gender signal when it is explicitly encoded in the surface form.
\end{itemize}

\subsection{Context Sampling}
For each target word \textit{w}, we retrieve up to n sentences containing w via substring matching. We use \textit{n} = 20
contexts per profession term and \textit{n} = 12 contexts per anchor term across all languages, to ensure stable cen-
troid estimation within CPU memory constraints. Words with fewer than the minimum available contexts
are excluded.


\subsection{Contextual to Static Reduction}
Transformer models produce contextualised token representations that vary across inputs. To enable geometric analysis, we adopt the reduction framework of \cite{Bommasani:2020}. For each occurrence of word $w$ in context $c_i$, subword representations are mean-pooled. The result is averaged across $n$ contexts. Formally, at layer $\ell$:
\begin{equation}
\label{eq:reduction}
s_\ell(w) = \frac{1}{n}\sum_{i=1}^n h_\ell(w, c_i),
\end{equation}
where $h_\ell(w, c_i)$ is the mean pooled subword representation. This yields one stable static embedding per layer for each target word.

\subsection{Gender Direction Construction}
Following \cite{Bolukbasi:2016}, we define a gender direction using predefined anchor sets. Let $M$ and $F$ denote the male and female anchor term sets. For each layer $\ell$, anchor representations are reduced following Eq. \eqref{eq:reduction}, and the centroids for the male and female anchor sets ($A_M$ and $A_F$) are defined as: 
\begin{equation}
c_{M,\ell} = \frac{1}{|M|}\sum_{w \in M} s_\ell(w), \quad c_{F,\ell} = \frac{1}{|F|}\sum_{w \in F} s_\ell(w),
\end{equation}
and the layer specific gender direction is:

\begin{equation}
g_\ell = c_{M,\ell} - c_{F,\ell}.
\end{equation}

This vector encodes the principal geometric contrast between male and female anchors at layer $\ell$ \parencite{Bolukbasi:2016}.

\subsection{Bias Measurement and Agreement}
For each profession $p$ and layer $\ell$ we compute two geometric estimators:
\begin{enumerate}[(a)]
    \item \textbf{Gender Direction Projection (\texttt{proj})}: The scalar projection of $s_\ell(p)$ onto the gender direction:
    \begin{equation}
    ProjBias_\ell(p) = \frac{s_\ell(p) \cdot g_\ell}{\|g_\ell\|}.
    \end{equation}
    Positive values indicate alignment toward the male centroid; negative values indicate alignment toward the
female centroid. The scalar projection preserves vector magnitude and is sensitive to the strength of the
gender association \parencite{Bolukbasi:2016}.
    \item \noindent \textbf{Centroid Cosine Difference (\texttt{cosdiff})}. As a complementary magnitude normalised estimator:

\begin{equation}
    \text{CosBias}_\ell(p) = \cos(\mathbf{s}_\ell(p), \mathbf{c}_{M,\ell}) - \cos(\mathbf{s}_\ell(p), \mathbf{c}_{F,\ell}),
\end{equation}

where cosine similarity is defined as \parencite{Manning:2008}:

\begin{equation}
    \cos(\mathbf{a}, \mathbf{b}) = \frac{\mathbf{a} \cdot \mathbf{b}}{\|\mathbf{a}\| \|\mathbf{b}\|} .
\end{equation}
\end{enumerate}


This estimator directly compares relative angular proximity to male and female centroids and is invariant
to vector scale.


\subsection{Estimator Agreement}
To assess measurement robustness, we compute Spearman rank correlation \parencite{Spearman:1904} between the two estimators across profession terms at each layer:
\begin{equation}
\rho_\ell = \text{Spearman}(\{ProjBias_\ell(p)\}_p, \{CosBias_\ell(p)\}_p).
\end{equation}

$\rho\ell$ = 1 indicates perfect ordinal agreement across professions.

\subsection{Layer Wise Aggregation}
For visualisation we compute the mean projection score across retained profession terms at each layer $\ell$,
yielding a layer wise bias trajectory:

\begin{equation}
    \overline{\text{Proj}}_\ell = \frac{1}{|P|} \sum_{p \in P} \text{ProjBias}_\ell(p),
\end{equation}

where \textit{P} is the set of retained professions. This trajectory summarises how the overall geometric gender
signal evolves across model depth.


\graphicspath{{figs/}}

\section{Results}\label{section-results}

\subsection{Estimator Agreement (RQ3)}

Before interpreting bias scores, we verify measurement robustness. Table~\ref{tab:spearman} reports the Spearman rank correlation $\rho$ between the projection and cosine-difference estimators for each model across all layers. Every model achieves a minimum $\rho \geq 0.97$ and a mean above $0.99$, showing a near perfect agreement. The one notable exception is XLM-V at layer~12, where $\rho$ drops to $0.70$, consistent with the representational collapse commonly observed at the final layer.

\begin{table}[!htbp]
\centering
\caption{Estimator agreement: Spearman $\rho$ between projection and cosine-difference scores across layers (Spanish).}
\label{tab:spearman}
\begin{tabular}{lccc}
\toprule
\textbf{Model} & \textbf{Min $\rho$} & \textbf{Max $\rho$} & \textbf{Mean $\rho$} \\
\midrule
XLM-R Base     & 0.991 & 1.000 & 0.997 \\
XLM-R Large    & 0.986 & 1.000 & 0.997 \\
XLM-V Base     & 0.700 & 1.000 & 0.976 \\
mDeBERTa-v3    & 0.967 & 0.999 & 0.990 \\
mBERT          & 0.979 & 0.998 & 0.990 \\     
\bottomrule
\end{tabular}
\end{table}

\subsection{Group A: Invariant Professions (RQ1)}

Table~\ref{tab:groupA} reports projection scores for the ten invariant professions. We report measurements at each model's peak layer (e.g., Layer~9 for XLM-R Base) where bias magnitude is highest. As shown in Figure~\ref{fig:layer_curves}, computing a mean across all layers dilutes the signal because early layers focus on syntax and final layers prepare logits for output. The full layer by layer trajectories are provided in subsequent heatmaps.

Somey terms show neutral morphology: \textit{dentista} ($+0.01$ in XLM-R Base) and \textit{cantante} ($-0.21$ in XLM-R Large) remain near zero. However, other invariant terms exhibit strong male bias. For instance, \textit{comerciante} scores $+2.69$ in XLM-R Base, while mDeBERTa classifies it neutrally ($-0.03$). \textit{Piloto} shows consistently high male bias across all evaluated models.

\begin{table}[!htbp]
\centering
\caption{Projection scores for Group~A (invariant professions) at each model's peak layer. Positive values indicate male bias.}
\label{tab:groupA}
\small
\begin{tabular}{lrrrrr}
\toprule
\textbf{Profession} & \textbf{XLM-R B} & \textbf{XLM-R L} & \textbf{XLM-V} & \textbf{mDeBERTa} & \textbf{mBERT} \\
 & \textbf{(L9)} & \textbf{(L16)} & \textbf{(L7)} & \textbf{(L5)} & \textbf{(L11)} \\
\midrule
policía       & $+2.09$ & $+0.85$ & $-0.77$ & $-0.22$ & $+1.12$ \\
artista       & $+3.08$ & $+2.14$ & $+1.49$ & $+0.64$ & $+5.04$ \\
comerciante   & $+2.69$ & $+1.41$ & $+2.36$ & $-0.05$ & $+3.71$ \\
periodista    & $+2.56$ & $+1.03$ & $+0.81$ & $+0.26$ & $+3.13$ \\
piloto        & $+3.33$ & $+1.78$ & $+2.03$ & $+1.22$ & $+4.72$ \\
cantante      & $+2.15$ & $-0.21$ & $+0.01$ & $+0.31$ & $+0.36$ \\
juez          & $+2.56$ & $+1.96$ & $+1.19$ & $+0.76$ & $+3.32$ \\
dentista      & $+2.08$ & $+1.41$ & $+1.02$ & $+0.19$ & $+3.00$ \\
estudiante    & $+2.40$ & $+1.74$ & $+1.35$ & $+0.76$ & $+4.43$ \\
\bottomrule
\end{tabular}
\end{table}


\subsection{Group B: Gendered Profession Pairs (RQ1)}\label{subsec:groupB}

Table~\ref{tab:groupB} presents peak layer projection scores for the masculine and feminine forms of some of the gendered profession, alongside their magnitude asymmetry ($\Delta = |proj_M| - |proj_F|$). Figure~\ref{fig:asymmetry} visualises this geometry for XLM-R Base.

While masculine forms align positively and feminine forms align negatively, the absolute magnitude of masculine scores is consistently higher. For example, in XLM-R Base at layer~9, \textit{profesor} scores $+3.45$ while \textit{profesora} scores $+0.08$ ($\Delta = 3.37$). This asymmetry indicates that masculine forms serve as the default geometric anchor for the profession concept, effectively treating feminine forms as semantic deviations. Figure~\ref{fig:gender_gap} further demonstrates that this massive disparity between masculine and feminine projections is not an isolated artifact of the peak layer, but a persistent mathematical reality across the entire depth of the architecture.

\begin{table}[!htbp]
\centering
\caption{Peak layer projection scores for Group~B gendered pairs (XLM-R Base, Layer~9). $\Delta = |proj_M| - |proj_F|$. Positive $\Delta$ indicates masculine dominance.}
\label{tab:groupB}
\small
\begin{tabular}{lrrr}
\toprule
\textbf{Profession} & \textbf{Masculine} & \textbf{Feminine} & $\boldsymbol{\Delta}$ \\
\midrule
abogado/a     & $+3.48$ & $+0.68$ & $+2.80$ \\
profesor/a    & $+3.45$ & $+0.08$ & $+3.37$ \\
mecánico/a    & $+2.94$ & $+1.15$ & $+1.80$ \\
cocinero/a    & $+2.90$ & $+0.56$ & $+2.34$ \\
minero/a      & $+2.66$ & $+0.90$ & $+1.76$ \\
arquitecto/a  & $+3.06$ & $+1.52$ & $+1.54$ \\
ingeniero/a   & $+3.15$ & $+0.88$ & $+2.27$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\linewidth]{fig3_asymmetry.pdf}
\caption{Masculine vs.\ feminine projection scores at peak layer~9 for XLM-RoBERTa Base.}
\label{fig:asymmetry}   
\end{figure}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\linewidth]{fig7_gender_gap.pdf}
\caption{Layer wise mean projections for masculine vs.\ feminine forms in XLM-RoBERTa Base. The masculine forms persistently dominate the semantic space across all network depths.}
\label{fig:gender_gap}   
\end{figure}


\subsection{Layer Wise Bias Trajectory (RQ2)}\label{subsec:layers}

Figure~\ref{fig:layer_curves} displays the mean projection score across representations at each transformer layer. XLM-R Base, XLM-R Large, and XLM-V share a rise peak decay trajectory: bias is weak initially, peaks in the middle layers (7 to 16), and decreases toward output generation. This matches observations that middle layers encode core semantic relationships.

In contrast, mDeBERTa-v3-base never develops the male biased signal seen in RoBERTa models but instead, its projection remains near zero or slightly negative across all layers. This architectural shift is analysed in Section~\ref{section-discussion}.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\linewidth]{fig1_layer_curves.pdf}
\caption{Layer wise mean gender projection across all professions for four Spanish models. The three RoBERTa family models show a rise, peak and decay pattern while mDeBERTa-v3 stays near or below zero.}
\label{fig:layer_curves}
\end{figure}


\subsection{Architecture-Driven Direction Reversal (RQ4)}

Figure~\ref{fig:model_comparison} contrasts per profession heatmaps for XLM-R Base and mDeBERTa-v3. While XLM-R Base displays positive male values in middle layers, mDeBERTa projects predominantly negative or near zero values.

Since both models use comparable training corpora, this results in a reversal likelihood from mDeBERTa's \textit{Disentangled Attention} mechanism. By separating content and positional matrices, the model mitigates the accumulation of gender associations from grammatical context into the core representations.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\linewidth]{fig6_model_comparison.pdf}
\caption{Per profession projection heatmaps: XLM-RoBERTa Base (left) vs.\ mDeBERTa-v3 (right). Disentangled Attention produces a reversal in bias direction across all professions. Layer~12 is excluded for mDeBERTa due to ELECTRA-head artifacts.}
\label{fig:model_comparison}
\end{figure}

The full per-profession heatmap for XLM-R Base (Figure~\ref{fig:heatmap}) illustrates the layered structure: Group~A terms cluster near the boundary between red and blue, while Group~B masculine terms are deep red and feminine terms are deep blue.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\linewidth]{fig2_heatmap_xlmr_base.pdf}
\caption{Full per profession heatmap for XLM-RoBERTa Base. Horizontal lines separate Group~A (invariant), Group~B masculine, and Group~B feminine professions.}
\label{fig:heatmap}
\end{figure}


\subsection{Cross Lingual Patterns (RQ1)}\label{subsec:crosslingual}

To contextualise these results, Figure~\ref{fig:crosslingual} maps the layer wise projection for XLM-RoBERTa Base across Spanish, Arabic, and Tigrinya, enabling a controlled comparison. Table~\ref{tab:crosslingual} provides key metrics.

Despite large disparities in pretraining corpus size and morphological structure, the bias trajectory is highly consistent. For all three languages, XLM-R Base produces a smooth rise that peaks precisely at Layer~9, with magnitudes bounded between $+2.15$ and $+2.99$. This indicates that the base architecture structures gender bias uniformly across resource levels.

\begin{table}[!htbp]
\centering
\caption{Cross lingual summary: layer wise projection statistics for 12-layer models.}
\label{tab:crosslingual}
\small
\begin{tabular}{llcccc}
\toprule
\textbf{Lang.} & \textbf{Model} & \textbf{Peak Layer} & \textbf{Peak Proj.} & \textbf{Min $\rho$} & $n$ (prof.) \\
\midrule
Spanish  & XLM-R Base & 9 & $+2.15$ & 0.991 & 25 \\
Arabic   & XLM-R Base & 9 & $+2.64$ & 0.964 & 23 \\
Tigrinya & XLM-R Base & 9 & $+2.99$ & 0.992 & 30 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\linewidth]{fig4_crosslingual.pdf}
\caption{Cross lingual layer wise mean projection for XLM-RoBERTa Base across Spanish, Arabic, and Tigrinya. Remarkably, the architecture encodes bias with an identical structural trajectory (peaking exactly at Layer~9) regardless of the language's resource level.}
\label{fig:crosslingual}
\end{figure}


% ─────────────────────────────────────────────────────────────────────────────
% ─────────────────────────────────────────────────────────────────────────────
\section{Discussion and Conclusions}\label{section-discussion}

\subsection{Grammatical Neutrality vs.\ Societal Frequency}
Multilingual transformers learn morphological neutrality without explicit supervision, locating invariant professions (\textit{dentista}, \textit{cantante}) near the geometric origin. However, statistical frequency in the pretraining corpus overrides grammatical rules for historically male dominated roles. For example, XLM-R Base projects the invariant \textit{comerciante} ($+2.69$) similarly to the explicitly masculine \textit{mecánico} ($+2.94$). Only mDeBERTa-v3 suppresses this effect ($-0.03$), indicating that architectural design also has an influence in the conflict between grammar and frequency.

\subsection{The Masculine Default}
The asymmetry analysis shows that masculine forms occupy a dominant position as the default representation of a profession. Feminine forms correctly orient toward the female centroid but remain closer to the origin. By preserving this data asymmetry or imbalance, the models treat masculine professions as more semantically central, carrying practical implications for certain applications like search retrieval or automated text analysis, and the interpretability research.

\subsection{Architecture over Pretraining Data}
Our results emphasize architecture as a dominant factor in bias encoding. mDeBERTa-v3 reverses the bias direction relative to RoBERTa-family models, preserving relative profession rankings ($\rho > 0.97$) but flipping their geometric orientation. We attribute this to Disentangled Attention, by separating content from positional context, the model prevents societal frequencies from having too much influence on the gender direction.

Similarly, the cross lingual analysis shows that semantic structures depend less on pretraining volume than often assumed. XLM-R Base encodes bias with an identical structural trajectory (peaking at Layer~9) across Spanish, Arabic, and Tigrinya, despite massive disparities in corpus size. This suggests the attention mechanism functions as a universal template for how abstractions like gender are structured.

\subsection{Final Remarks and Limitations}
Since we only looked at base embeddings without fine tuning, it's possible these bias patterns could change once the models are adapted for specific tasks. Moreover, our anchor sets and Spanish context corpus may not capture all domains of language use. 

In conclusion, multilingual transformers encode measurable geometric gender bias that peaks in the middle semantic layers. Evaluating fairness exclusively at the final layer might underestimate magnitude. Our results suggest that measuring geometric structures across layers is more reliable. Furthermore, architectural choices like attention factorisation seem to inherently mitigate social bias, bypassing the need for explicit debiasing pipelines.

\subsection*{Reproducibility Statement}
To facilitate full reproducibility, the source code, data parsing scripts, and complete evaluation pipeline used in this study are publicly available in our GitHub repository: \url{https://github.com/JuanArango99/ml4nlu-gender-bias}.


% END

% ─────────────────────────────────────────────────────────────────────────────
\appendix
\section*{Appendix A. Notable Individual Model Trajectories}
\addcontentsline{toc}{section}{Appendix A. Notable Individual Model Trajectories}

While the main text aggregates layer wise projections or restricts analysis to XLM-RoBERTa Base to uncover structural consistency, individual models exhibit distinct edge case behaviors. We highlight four examples below.

\begin{figure}[!htbp]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{../output/ti/bert-base-multilingual-cased/figs/ti_mbert_projection_curve.png}
        \caption{Tigrinya (mBERT): Extreme low-resource collapse. With insufficient pretraining data, mBERT fails to generate a societal frequency signal, collapsing into arbitrary negative projections.}
        \label{fig:app_ti_mbert}
    \end{minipage}\hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{../output/ar/xlm-roberta-large/figs/ar_xlmr_large_projection_curve.png}
        \caption{Arabic (XLM-R Large): Late stage peaking. While Base models peak near layer~9, this Large model peaks at layer~14 with a $+4.54$ magnitude.}
        \label{fig:app_ar_xlmrl}
    \end{minipage}
\end{figure}

\begin{figure}[!htbp]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{../output/es/microsoft_mdeberta-v3-base/figs/es_mdeberta_projection_curve.png}
        \caption{Spanish (mDeBERTa-v3): Disentangled Attention reversal. The projection score stays negative across all middle layers, reversing the RoBERTa upward curve.}
        \label{fig:app_es_mdeb}
    \end{minipage}\hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{../output/es/facebook_xlm-v-base/figs/es_xlmv_base_projection_curve.png}
        \caption{Spanish (XLM-V Base): Final layer artifact spike. The bias score exhibits an unnatural spike at layer~12 ($+3.11$) possibly due to token level output preparation.}
        \label{fig:app_es_xlmv}
    \end{minipage}
\end{figure}

\section*{Appendix B. Evaluated Lexicons (Spanish)}
\addcontentsline{toc}{section}{Appendix B. Evaluated Lexicons (Spanish)}

This section details the explicit Spanish word sets used to construct the gender subspace and evaluate profession embeddings. The anchor sets (Table~\ref{tab:app_anchors_es}) capture fundamental gender semantics, while the profession sets (Table~\ref{tab:app_professions_es}) are divided into morphologically invariant (Group~A) and gender-inflected (Group~B) terms.

\begin{table}[!htbp]
\centering
\caption{Spanish gender anchor sets used to define the semantic subspace.}
\label{tab:app_anchors_es}
\small
\begin{tabular}{ll}
\toprule
\textbf{Masculine Anchors} & \textbf{Feminine Anchors} \\
\midrule
hombre & mujer \\
niño & niña \\
padre & madre \\
él & ella \\
hermano & hermana \\
esposo & esposa \\
abuelo & abuela \\
hijo & hija \\
tío & tía \\
varón & señora \\
chico & chica \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[!htbp]
\centering
\caption{Spanish profession terms evaluated in this study, categorized by morphological group.}
\label{tab:app_professions_es}
\small
\begin{tabular}{lll}
\toprule
\textbf{Group A (Invariant)} & \textbf{Group B (Masculine)} & \textbf{Group B (Feminine)} \\
\midrule
policía & abogado & abogada \\
recepcionista & enfermero & enfermera \\
artista & agricultor & agricultora \\
comerciante & profesor & profesora \\
periodista & mecánico & mecánica \\
piloto & cocinero & cocinera \\
cantante & limpiador & limpiadora \\
juez & minero & minera \\
dentista & arquitecto & arquitecta \\
estudiante & ingeniero & ingeniera \\
\bottomrule
\end{tabular}
\end{table}

% -----------------------------

%
% references
%
\newpage
\printbibliography

\end{document}